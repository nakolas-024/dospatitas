---
// src/components/ProductModal.astro (SIN SCRIPT)
---
<div id="product-modal" class="modal-overlay hidden" aria-hidden="true">
  <div class="modal-content" role="dialog" aria-modal="true">
    <button id="modal-close-btn" class="modal-close-btn" aria-label="Cerrar modal">×</button>
    
    <div id="modal-product-content" class="product-view">
        <div class="img-wrapper">
            <img id="modal-img" src="" alt="Imagen del producto" />
        </div>
        <h3 id="modal-title"></h3>
        <button id="add-to-cart-modal-btn" class="add-to-cart-modal">Añadir al carrito 🛒</button>
    </div>
  </div>
</div>

<style>
/* Estilos del Modal (sin cambios) */
.modal-overlay {
  position: fixed; top: 0; left: 0; width: 100%; height: 100%;
  background-color: rgba(0,0,0,0.6);
  backdrop-filter: blur(5px);
  display: flex; align-items: center; justify-content: center;
  z-index: 2000;
  opacity: 0; pointer-events: none;
  transition: opacity 0.3s ease;
}
.modal-overlay:not(.hidden) { opacity: 1; pointer-events: auto; }
.modal-content {
  background-color: #fff; padding: 1rem;
  border-radius: 20px; max-width: 500px; width: 90%;
  position: relative; box-shadow: 0 15px 40px rgba(0,0,0,0.25);
  transform: scale(0.95); opacity: 0;
  transition: transform 0.3s ease, opacity 0.3s ease;
}
.modal-overlay:not(.hidden) .modal-content { transform: scale(1); opacity: 1; }
.modal-close-btn {
  position: absolute; top: 0.5rem; right: 0.5rem;
  background: #f1f1f1; border-radius: 50%;
  width: 40px; height: 40px; border: none; font-size: 1.8rem;
  color: #555; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: transform 0.2s ease, background-color 0.2s ease;
}
.modal-close-btn:hover { transform: rotate(90deg); background-color: #e0e0e0; }
.product-view { padding: 1rem; text-align: center; }
.product-view .img-wrapper { margin-bottom: 1rem; }
.product-view img { width: 100%; max-height: 400px; object-fit: contain; border-radius: 12px; }
.product-view h3 { font-size: 1.5rem; font-weight: 700; color: #333; margin-bottom: 1rem; }
.add-to-cart-modal {
  background: #f0ce0c; color: #333; font-weight: 700;
  padding: .8rem 1.6rem; border-radius: 50px; border: none;
  cursor: pointer; transition: background .25s ease;
  width: 100%; font-size: 1rem;
}
.add-to-cart-modal:hover { background: #fc9700; color: #fff; }
.add-to-cart-modal:disabled { background-color: #4caf50; color: white; cursor: not-allowed; }
</style>